"use client";
import { useI18n } from "../i18n/client";
export default function StaticPreflightChecklist({snapshot}:{snapshot:unknown}){const {t}=useI18n();if(!snapshot||typeof snapshot!=="object")return <p className="readiness-blocker">{t("Blocker: production specification snapshot missing")}</p>;const spec=snapshot as Record<string,unknown>;const types=Array.isArray(spec.accepted_file_types)?spec.accepted_file_types.join(", "):t("Confirm with operator");return <details><summary>{t("Static preflight checklist")}</summary><ul><li>{t("Accepted files")}: {types}</li><li>{t("Minimum DPI")}: {String(spec.minimum_dpi??t("manual check"))}</li><li>{t("Colour space")}: {String(spec.colour_space??t("manual check"))}</li><li>{t("Bleed")}: {String(spec.bleed_mm??t("manual check"))} mm</li><li>{t("Fonts, outlines, and effective DPI require manual review.")}</li></ul></details>}
